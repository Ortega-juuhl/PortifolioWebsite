// Function to scroll smoothly to the top of the page
function scrollToTop() {
  window.scrollTo({
      top: 0, // Scroll to the top of the page
      behavior: 'smooth' // Enable smooth scrolling behavior
  });
}

// Function to scroll smoothly to the 'About' section
function scrollToAbout() {
  window.scrollTo({
      top: 670, // Scroll to the 'About' section at a specified vertical position
      behavior: 'smooth' // Enable smooth scrolling behavior
  });
}

// Function to scroll smoothly to the 'Projects' section
function scrollToProjects() {
  window.scrollTo({
      top: 1570, // Scroll to the 'Projects' section at a specified vertical position
      behavior: 'smooth' // Enable smooth scrolling behavior
  });
}

// Function to scroll smoothly to the 'Contact' section
function scrollToContact() {
  window.scrollTo({
      top: 2270, // Scroll to the 'Contact' section at a specified vertical position
      behavior: 'smooth' // Enable smooth scrolling behavior
  });
}
